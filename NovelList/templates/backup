{% extends 'base.html' %}
{% block content %}
<div class = 'top'>
<form method="POST" class="c-addform" autocomplete="off" action="{{ url_for('add_book') }}">
    <div class="c-row">
            <input type="text" maxlength="200" id='title'  placeholder="Tytuł" name="title" required>

            <select name="book_type" class="custom-select" id="book_type">
                {% for book_type in book_types %}
                <option value="{{ book_type }}">{{ book_type }}</option>
                {% endfor %}
            </select>

            <select name="status" id="status">
                {% for status in statuses %}
                <option value="{{ status }}">{{ status }}</option>
                {% endfor %}
            </select>

            <input type="number" required name="total_chapters" id="total_chapters" min="1" max="99999" placeholder="Rozdziały">

            <input type="text" name="last_chapter" placeholder="v12c8" id="last_chapter">

            <input type="number" name="grade"  id="grade" min="0" max="11" placeholder="0-11">

            <input type="number" name="priority" id="priority" min="-100" max="100" placeholder="-100-100">
    </div>

    <input type="url" name="link" id="link"  maxlength="400" placeholder="Link" pattern="((([A-Za-z]{3,9}:(?:\/\/)?)(?:[-;:&=\+\$,\w]+@)?[A-Za-z0-9.-]+(:[0-9]+)?|(?:www.|[-;:&=\+\$,\w]+@)[A-Za-z0-9.-]+)((?:\/[\+~%\/.\w\-_]*)?\??(?:[-\+=&;%@.\w_]*)#?(?:[\w]*))?)">
        
    <textarea name="notes" maxlength="150" id="notes" placeholder="Notatka"></textarea>

    <button type="submit" value="Add Book">Dodaj</button>
        
</form>
</div>
{% endblock %}